public class StackIntro {
    // Recursively defined data structures:
    // recall that a recursive method is a method that is defined in terms of its self (calls itself)
    // A recursiely defined class is a class where one of its fields is an instance of itself

    // We could define a stack of pancakes as being 2 things:
    //1. a Pancake
    //2. A stack of pancakes below it

    //Using an inner class to represent one node of a stack

    //Pop the top of the stack:
    // save the top data into a variable and move the overall top down to the stack bellow and return the saved data
}
